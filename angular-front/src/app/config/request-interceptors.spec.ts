import { RequestInterceptorService } from './request-interceptors';

describe('RequestInterceptors', () => {
  it('should create an instance', () => {
    expect(new RequestInterceptorService()).toBeTruthy();
  });
});
